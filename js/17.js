// Diferencia de un metodo y una funcion

const numero1 = 20;
const numero2 ='20';

console.log(parseInt(numero2)) // Parseint es una funcion que convierte un string a numero si es valido
console.log(numero1.toString()); // toString es un metodo de numero a string