// Objeto Math

// en la consola del navegador escribimos window podemos encontrar todas las funciones de javaScript

let resultado;


resultado = Math.PI; //nos trae el valor de PI 3.14
resultado = Math.round(2.5); //round sirve para redondear 
resultado = Math.ceil(2.2); //ceil redondea para arriba sin importar el numero dspues del punto
resultado = Math.floor(2.2); //redondea para abajo lo contrario de ceil
resultado = Math.sqrt(144); //la raiz cuadrada
resultado = Math.abs(-200); //convierte el numero en positivo
resultado = Math.min(3, 5, 1, 8, 2, 10); //min nos da el numero mas pequeño
resultado = Math.max(3, 5, 1, 8, 2, 10); //maz nos da el numero mas alto
resultado = Math.random() //nos da un numero randon se queda en menos de 1
resultado = Math.floor(Math.random() * 30); //de prueba para que nos genere un numero randon


console.log(resultado)